import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        minWidth: 200,
    },
    dropDownStyle: {
        textAlign: 'center',
    }
});

export default styles;
